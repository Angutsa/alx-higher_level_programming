#!/usr/bin/python3

def update_dictionary(a_dictionary, key, value):
    """Adds or replaces a key/value in the dictionary"""

    new_dict = a_dictionary
    new_dict[key] = value
    return (new_dict)
